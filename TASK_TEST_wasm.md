# Task: Add tests to src/wasm.rs (currently 2 tests)

Read src/wasm.rs first. Then add 6+ more tests to the existing #[cfg(test)] mod.

Write tests for:
- WebAssembly compilation functions
- Error handling for invalid paths
- Any public API functions
- Edge cases

Add new tests into the EXISTING #[cfg(test)] mod.

Run `cargo test wasm` to verify all pass. Fix errors.
